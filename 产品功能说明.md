# AI 保险教练 — 完整功能逻辑文档

## 一、产品定位

面向 **保险代理人** 的 AI 策略参谋工具。代理人将面谈中遇到的客户情况填入 KYC 表单，AI 基于一套完整的顾问式销售方法论，给出 **卡点诊断 → 推演方向 → 建议话术** 三段式指导。

## 二、技术架构

| 层 | 技术 |
|----|------|
| 前端 | Next.js 16 + React 19 + TypeScript + Tailwind CSS 4 |
| 后端 | Next.js API Route Handlers（同仓库，22 个 API 接口） |
| AI | DeepSeek API（通过 openai 兼容 SDK 调用），流式输出 |
| 数据库 | Neon Serverless PostgreSQL（@neondatabase/serverless），通过 DATABASE_URL 连接 |
| 认证 | iron-session（cookie-based）+ Bearer Token（小程序端）。手机号 + 验证码登录，小程序也支持微信手机号一键授权 |
| 部署 | Vercel / 单机 Node.js 服务 |

## 三、用户操作流程

### 1. 登录
- 输入手机号 + 验证码（开发阶段固定 1234）
- 新用户自动注册并赠送 **10 积分**
- 每次 AI 对话消耗积分（按实际 token 用量计算，约 1 分），积分不足时 API 返回 402 提示充值
- 积分页支持手动充值 / 微信支付充值
- 小程序端支持微信手机号一键登录（getPhoneNumber）

### 2. 填写 KYC 表单（5 步向导）
代理人在表单中录入当前面谈客户的信息，分为 5 个步骤。
- 支持客户档案功能：可为每个客户创建独立档案，多次面谈数据关联到同一客户
- 表单数据自动保存到 localStorage，刷新页面可恢复草稿

### 3. AI 教练诊断
点击"提交给保险教练诊断"后：
1. 前端将表单 JSON 发送给 POST /api/coach，服务端统一拼接为结构化文本
2. API 根据 KYC 数据动态选择知识模块（从 20+ 个模块中按场景裁剪，约 2500-4000 token），而非整本知识库全量加载
3. AI 通过流式输出（SSE，Web 端）或 JSONL（小程序端）实时显示诊断结果
4. 对话自动保存到历史记录

### 4. 追问
诊断完成后，可在输入框追问（补充客户信息、反馈面谈结果等），AI 结合历史上下文继续给出建议。
- AI 每次回复末尾会生成 2-3 个引导式追问按钮，点击可快速提问

### 5. 历史对话
可通过侧边栏查看、加载历史诊断对话。
- 每条对话可标记状态：跟进中 / 已成交 / 已流失

### 6. AI 反馈与导出
- 每条 AI 回复下方有「有帮助」「没帮助」按钮，收集诊断质量反馈
- 支持一键复制诊断内容为纯文本 / 打印导出

## 四、KYC 表单 — 5 步骤字段详情

### 步骤 1：客户画像与生活状态（10 个字段）
| # | 字段名 | 中文标签 | 类型 | 选项/说明 |
|---|--------|----------|------|-----------|
| 1 | clientName | 名称 | 文本 | 如"张先生""李姐" |
| 2 | gender | 性别 | 单选 | 男 / 女 |
| 3 | age | 年龄 | 数字 | 范围 0–120 |
| 4 | city | 所在城市 | 文本 | 如"上海" |
| 5 | healthCondition | 身体情况 | 文本区 | 如"健康 / 有高血压" |
| 6 | maritalStatus | 婚姻状况 | 单选 | 未婚/已婚/离异/丧偶/再婚 |
| 7 | childrenDetail | 子女详情 | 文本区 | 如"儿子/8岁/公立小学" |
| 8 | parentsDetail | 父母情况 | 文本区 | 是否健在/赡养责任 |
| 9 | personality | 性格特征 | 文本区 | MBTI / 沟通偏好 |
| 10 | hobbies | 兴趣爱好 | 文本区 | 如"马拉松/高尔夫" |

### 步骤 2：工作与收支（12 个字段）
| # | 字段名 | 中文标签 | 类型 | 选项/说明 |
|---|--------|----------|------|-----------|
| 1 | clientIndustry | 行业 | 下拉 | 20 个行业选项（GB/T 4754 分类） |
| 2 | clientCompany | 公司 | 文本 | 如"字节跳动" |
| 3 | clientPosition | 职责&职位 | 文本 | 如"技术专家" |
| 4 | careerDevelopment | 职业发展空间 | 下拉 | 稳定上升期/瓶颈期/创业波动/已退休 |
| 5 | breadwinner | 家庭经济支柱 | 下拉 | 客户本人/配偶/夫妻共同/父母 |
| 6 | incomeSources | 主要收入来源 | 文本 | 工资/经营/房租/投资分红 |
| 7 | annualIncome | 家庭年收入 | 数字 | 万元 |
| 8 | spouseIndustry | 配偶行业 | 下拉 | 同行业选项 |
| 9 | spouseCompany | 配偶公司 | 文本 | |
| 10 | spousePosition | 配偶职责&职位 | 文本 | |
| 11 | monthlyExpense | 月度固定支出 | 数字 | 万元，含房贷/车贷 |
| 12 | majorExpensePlan | 未来大额支出计划 | 文本区 | 购房/教育/医疗/养老 |

### 步骤 3：资产情况（8 个字段）
| # | 字段名 | 中文标签 | 类型 | 选项/说明 |
|---|--------|----------|------|-----------|
| 1 | fixedAssets | 固定资产 | 文本区 | 如"自住房1套" |
| 2 | liquidAssets | 流动资产合计 | 数字 | 现金+存款+短期理财 |
| 3 | liabilities | 负债情况 | 文本区 | 房贷/信用卡 |
| 4 | investmentAmount | 投资金额 | 数字 | 股票/基金/股权 |
| 5 | investmentStyle | 投资偏好 | 下拉 | 保守/稳健/进取 |
| 6 | riskTolerance | 风险承受能力 | 下拉 | 低/中/高 |
| 7 | expensePressure | 支出压力感知 | 下拉 | 无压力/房贷压力/教育开销大/存不下钱 |

### 步骤 4：已有保障（4 个字段）
| # | 字段名 | 中文标签 | 类型 | 选项/说明 |
|---|--------|----------|------|-----------|
| 1 | protectionInsurance | 保障类保险 | 文本区 | 医疗/重疾/意外/定寿合并填写 |
| 2 | savingsInsurance | 储蓄类保险 | 文本区 | 年金/增额终身寿合并填写 |
| 3 | insuranceAttitude | 对保险的态度 | 下拉 | 满意/不清楚/不够/没买过/排斥 |
| 4 | otherInsurance | 其他保险 | 文本 | 企业团体险/惠民保 |

### 步骤 5：面谈入口与卡点（5 个字段）
| # | 字段名 | 中文标签 | 类型 | 选项/说明 |
|---|--------|----------|------|-----------|
| 1 | triggerScenario | 触发场景 | 下拉 | 客户主动咨询 / 非主动咨询 |
| 2 | clientOriginalWords | 客户原话或背景描述 | 文本区 | |
| 3 | pastInteraction | 历史互动摘要 | 文本区 | 之前的互动背景 |
| 4 | clientObjection | 客户异议/卡点 | 文本区 | |
| 5 | agentResponse | 代理人回应 | 文本区 | |

## 五、知识库：顾问式销售方法论

当前知识库约 8000 字，已拆分为 20+ 个模块，按 core/method/reference 三层分级。
**不是整本全量加载**——根据 KYC 数据动态选择相关模块（约 2500–4000 token），由 `src/lib/knowledge.ts` 中的 `selectModules()` 函数实现。

选择逻辑：
- 基础模块（角色定位、输出结构、面谈流程图）始终加载
- 触发场景含"主动"→ 加载主动咨询面谈流程；含"非主动"→ 加载非主动「拆墙法」
- 根据收入/资产判断客户金字塔层级，只加载对应层级模块
- 按卡点关键词匹配对应异议处理模块
- 子女信息触达教育金模块

| 类别 | 模块数 | 核心内容 |
|------|--------|----------|
| 总纲 | 1 | 面谈阶段流程图 + 禁止跳步规则（卖水模型） |
| 角色与原则 | 1 | 角色定位、7 条工作原则（含 3 条红线）、输出结构 |
| 认知转型 | 1 | 强关系→弱关系、客户群代际变化、卖水模型 |
| 客户定位 | 5 | 五级金字塔，每层画像/需求/策略 |
| 社交激活 | 1 | 社交线索原则、微信资源梳理、朋友圈激活 |
| 线上转面谈 | 1 | 电话邀约四步法 |
| 面谈流程 | 3 | 找需求、强化需求、找不足给方案 |
| 储蓄险风险 | 1 | 四大风险精讲 |
| 异议处理 | 6 | 三原则 + 六大高频异议逐个击破 |
| 复盘体系 | 1 | 面谈后复盘模板 |

## 六、提示词拼接逻辑（服务端生成）

前端不再自己拼接文本——只发送原始 KYC JSON 数据给 `/api/coach`，由服务端的 `buildKycContext()` 统一拼接为结构化 Markdown。

```text
# 当前客户档案

## 客户画像与生活状态
- 名称：张先生
- 年龄：35 / 性别：男
- 所在城市：上海 / 婚姻状况：已婚
- 身体情况：健康
- 子女详情：儿子/6岁/公立小学
- ...

## 工作与收支
- 行业：互联网 / 公司：字节跳动
- ...

## 资产情况
...

## 已有保障
...

## 面谈入口
...

## 当前卡点
...
```

## 七、配套功能

### 7.1 积分系统
- 初始 10 分（新用户注册时自动赠送）
- 每次 AI 对话按实际 token 用量扣减（约 1 分），在流式输出完成后扣
- 积分不足时 API 返回 402，前端弹窗提示充值
- 积分页 `/points`：显示余额 + 充值（手动/微信支付） + 积分流水列表

### 7.2 客户档案管理 `/clients`
- 客户独立页面，列出所有客户
- 每个客户关联其所有历史对话
- 支持编辑客户名称、查看 KYC 快照
- 提交诊断时可选择已有客户或自动创建新客户

### 7.3 微信支付 `/payment`
- 管理员在后台生成充值订单
- 用户扫码支付，微信回调通知自动充值积分
- 订单状态：pending → paid / cancelled / expired

### 7.4 AI 反馈收集 `/api/feedback`
- 每条 AI 回复可评分 helpful / unhelpful
- 反馈数据入库供后期分析模型效果

### 7.5 移动端适配
- 桌面端：左右分栏（左侧 KYC 表单 + 右侧教练面板）
- 移动端：顶部标签切换（信息填写 / 教练指导）
- 表单区、教练区、历史对话面板均做了响应式处理

## 八、数据库表结构（Neon PostgreSQL）

```sql
-- 用户表
users (id SERIAL PK, phone UNIQUE, wechat_openid UNIQUE, created_at, last_login_at)

-- 客户表
clients (id SERIAL PK, user_id FK→users, name, kyc_snapshot TEXT, created_at, updated_at)

-- 积分流水表
point_transactions (id SERIAL PK, user_id FK→users, amount, type CHECK(charge/consume), description, created_at)

-- 对话记录表
conversations (id SERIAL PK, user_id FK→users, client_id FK→clients, title,
  messages TEXT(加密JSON), status CHECK(active/won/lost),
  total_input_tokens, total_output_tokens, created_at, updated_at)

-- AI 反馈表
feedbacks (id SERIAL PK, user_id FK→users, conversation_id FK→conversations,
  message_idx, rating CHECK(helpful/unhelpful), created_at)

-- 支付订单表
payment_orders (id SERIAL PK, user_id FK→users, order_no UNIQUE, points,
  amount_cents, status CHECK(pending/paid/cancelled/expired),
  payment_method, payment_ref, admin_note, paid_at, created_at, expires_at)
```

## 九、安全措施

| 措施 | 实现位置 |
|------|----------|
| Prompt 注入检测 | `/api/coach` — 13 条正则匹配，覆盖 KYC 数据和用户追问 |
| 速率限制 | `src/lib/rate-limit.ts` — 内存滑动窗口，coach/send-code/login 各自独立阈值 |
| 会话数据加密 | `src/lib/crypto.ts` — AES-256-GCM 加密对话内容 |
| HTTP 安全头 | `src/middleware.ts` — CSP / HSTS / X-Frame-Options / Referrer-Policy |
| 双通道认证 | `src/lib/auth.ts` — Web 端 iron-session Cookie + 小程序端 Bearer Token |
| 请求体大小限制 | `src/middleware.ts` — API 路由 100KB 上限 |

## 十、已实现 / 待优化清单

| 项目 | 状态 |
|------|------|
| KYC 表单字段覆盖（5 步 39 字段） | ✅ 已实现 |
| 知识库模块化动态裁剪 | ✅ 已实现 |
| 客户档案管理（长期追踪） | ✅ 已实现 |
| 微信支付积分充值 | ✅ 已实现 |
| 三段式诊断输出 | ✅ 已实现 |
| AI 反馈收集 | ✅ 已实现 |
| 导出报告（复制/打印） | ✅ 已实现 |
| 引导式追问选项 | ✅ 已实现 |
| 积分事务保护 | ⏳ 待优化（见实施计划第五批） |
| 错误处理白盒化 | ⏳ 待优化（见实施计划第五批） |
| 多模型支持 | 🔮 待讨论 |

详见 `实施计划.md` 中 13 项优化的分批实施方案。
